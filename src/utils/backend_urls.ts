import { API_URL } from "../config/config";

export const backend_url = {
  mainBannerURL: `${API_URL}main-banner/all`,
  mainBannerShortCurURL: `${API_URL}main-shortcut/all`,
  collectionsURL: `${API_URL}collections`,
};
